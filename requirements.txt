Flask==3.1.2
Werkzeug==3.1.3
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.3.0
blinker==1.9.0

# Transliteration support (optional - app will work without it for HK input)
# aksharamukha==2.3
# Note: aksharamukha has complex dependencies that may not work on Vercel
# The app is designed to work without it (graceful degradation)
